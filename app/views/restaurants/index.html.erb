<h1>All Restaurants</h1>
<div>
  <% @restaurants.each do |restaurant| %>
      <%= render restaurant %>  <!-- this will use _restaurant.html.erb -->
  <% end %>
</div>

<%= link_to "Add a new restaurant", new_restaurant_path, class: 'btn btn-primary p-2 m-2' %>
